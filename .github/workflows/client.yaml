on: [push]

jobs:
  mi-job:
    uses: ValentinaAstudillo/AUY1104-ValentinaAstudillo-Runner/.github/workflows/deploy-api.yaml@main
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}